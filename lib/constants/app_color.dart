import 'package:flutter/material.dart';

class AppColors {
  // In that we go the Figma and copy to the color code.
  // When we use own color code so add in the starting "0xff" then paste our code.
  static Color orangeColor = Color(0xffFFA451);
}